# Auth -- choose one mode, never both
OIDC_ENABLED=true
OIDC_CONFIG_URL=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_BASE_URL=
JWT_SIGNING_KEY=
STORAGE_ENCRYPTION_KEY=

# Or use bearer token instead of OIDC
# MCP_API_KEY=

# SMTP relay (optional overrides, defaults: localhost:25)
# SMTP_HOST=
# SMTP_PORT=
# MAIL_FROM=
